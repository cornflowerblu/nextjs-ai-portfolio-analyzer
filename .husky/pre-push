echo "🔍 Running pre-push checks..."

# Run linter
echo "🧹 Linting code..."
npm run lint || exit 1

# Run tests
echo "📝 Running tests..."
npm test -- --run || exit 1

# Run build
echo "🔨 Building project..."
NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS=1 npm run build || exit 1

echo "✅ Pre-push checks passed!"
